apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "goplatform.fullname" . }}
  labels:
    {{- include "goplatform.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
